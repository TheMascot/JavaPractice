spring:
  application:
    name: taskmanager
  datasource:
    url: jdbc:mysql://localhost:3306/taskmanager
    username: root
    password: test1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
